<!--components/goods-item/goods-item.wxml-->

<view class="goods-item">
  <image class="image" src="{{item.show.img}}" mode="scaleToFill"/>

  <view class="desc-info">
    <view class="title">{{item.title}}</view>
    <view class="info">
      <text class="price">￥{{item.price}}</text>
      <image class='icon' src="/assets/common/collect.svg"  lazy-load/>
      <text class="cfav">{{item.cfav}}</text>
    </view>
  </view>

</view>
 
